# Vercel Environment Configuration
# Add these to your Vercel project settings under Environment Variables

# NextAuth Configuration
NEXTAUTH_URL=https://your-app.vercel.app
NEXTAUTH_SECRET=your-secure-secret-generated-with-openssl-rand-base64-32

# Google OAuth (same as local development)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# External MongoDB on Hetzner
# Format: mongodb://username:password@your-hetzner-server-ip:27017/database
MONGODB_URI=mongodb://your-username:your-password@your-hetzner-ip:27017/data-wiz

# External Redis on Hetzner  
# Format: redis://username:password@your-hetzner-server-ip:6379
REDIS_URL=redis://your-username:your-password@your-hetzner-ip:6379

# Production Environment
NODE_ENV=production

# Vercel-specific optimizations
VERCEL_URL=${VERCEL_URL}
VERCEL_ENV=${VERCEL_ENV}

# Python Configuration for Vercel
PYTHON_PATH=/usr/bin/python3

# Performance Settings
MAX_FILE_SIZE=50MB
BATCH_PROCESSING_THRESHOLD=50000
CACHE_TTL=3600

# Security Settings
ALLOWED_ORIGINS=https://your-app.vercel.app
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# Database Connection Pool Settings
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=2
MONGODB_CONNECT_TIMEOUT=30000
MONGODB_SOCKET_TIMEOUT=30000

# Redis Connection Settings
REDIS_CONNECT_TIMEOUT=10000
REDIS_COMMAND_TIMEOUT=5000
REDIS_MAX_RETRIES=3